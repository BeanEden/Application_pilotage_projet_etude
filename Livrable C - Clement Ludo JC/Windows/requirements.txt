# =============================================================================
# NovaRetail — Pipeline Bronze/Silver/Gold
# Microsoft Fabric Lakehouse · PySpark · Docling
#
# Usage :
#   Option 1 (recommandée) : ajouter ce fichier dans
#     Workspace settings → Data Engineering → Environments → [env] → Libraries
#   Option 2 : coller dans le pool Spark
#     Workspace settings → Data Engineering → Spark settings → Manage packages
#   Option 3 : session uniquement (à éviter en prod)
#     %pip install -r requirements.txt --quiet
#
# NB : PySpark, Delta, mssparkutils et IPython sont fournis nativement
#      par le runtime Fabric — ne pas les ajouter ici.
# =============================================================================

# ── NB_01 Bronze — parsing multi-format ──────────────────────────────────────

# Parsing universel DOCX / PDF / XLSX / PPTX / HTML
# Tire python-docx, pypdfium2, pillow, lxml en dépendances transitives
docling>=2.0,<3.0

# Extraction archives .7z (canal SharePoint)
py7zr>=0.20,<1.0

# ── Dépendances explicitement utilisées dans le code ─────────────────────────
# (normalement tirées par docling, listées pour épingler les versions si besoin)

# Manipulation DOCX (pré-validation avant appel Docling)
# python-docx>=1.1.0

# Backend PDF Docling (résolu dynamiquement dans le parser)
# pypdfium2>=4.0.0
